---
import RunbookLayout from "../../../layouts/RunbookLayout.astro";
import RunbookTeaser from "../../../layouts/RunbookTeaser.astro";
---

<RunbookLayout
  title="Runbook 06 — Release gates (time-to-signal)"
  description="Preview of Runbook 06 — release gate contracts, smoke vs regression policy, and deterministic decision outputs."
  runbookNum="06"
>
  <RunbookTeaser
    num="06"
    title="Release gates (time-to-signal)"
    intro="Most test suites report failures. This runbook turns failures into decisions. It defines release gates as executable contracts that produce deterministic GO, WARN, or BLOCK outcomes based on real system behavior."
    bullets={[
      "Gate metadata encodes capability, impact, and ownership",
      "SMOKE suite provides fast release signal; REGRESSION provides depth and coverage",
      "CI output includes a complete decision contract with no interpretation or rerun required",
    ]}
    proves="Release decisions become system-driven, auditable, and repeatable—removing ambiguity from the deployment process."
    accessText="The full runbook includes gate structure, decision policy, contract output, and CI integration patterns used to produce reliable time-to-signal."
  />
</RunbookLayout>
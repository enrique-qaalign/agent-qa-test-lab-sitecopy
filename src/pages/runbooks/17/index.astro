---
import RunbookLayout from "../../../layouts/RunbookLayout.astro";
import RunbookTeaser from "../../../layouts/RunbookTeaser.astro";
---

<RunbookLayout
  title="Runbook 17 — Release gate agent"
  description="Preview of Runbook 17 — explicit GO, WARN, and BLOCK decisions driven by risk, capability impact, and suite intent."
  runbookNum="17"
>
  <RunbookTeaser
    num="17"
    title="Release gate agent"
    intro="Test results alone do not provide release confidence. This runbook defines a deterministic release gate that converts test outcomes into explicit GO, WARN, or BLOCK decisions based on risk, capability impact, and suite intent."
    bullets={[
      "Decisions are driven by enforceable policy, not interpretation",
      "Smoke and regression signals are evaluated with distinct intent",
      "Every decision includes rationale tied to business capability",
    ]}
    proves="Release decisions can be systematized—eliminating ambiguity and enabling consistent, risk-based delivery."
    accessText="The full runbook includes decision policy, capability risk mapping, output schema, and CI integration patterns for auditable release control."
  />
</RunbookLayout>
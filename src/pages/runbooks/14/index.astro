---
import RunbookLayout from "../../../layouts/RunbookLayout.astro";
import RunbookTeaser from "../../../layouts/RunbookTeaser.astro";
---

<RunbookLayout
  title="Runbook 14 — Structured failure artifacts"
  description="Preview of Runbook 14 — machine-readable failure artifacts, linked evidence, and deterministic diagnostics."
  runbookNum="14"
>
  <RunbookTeaser
    num="14"
    title="Structured failure artifacts"
    intro="Test failures are typically inconsistent and difficult to analyze without reruns. This runbook defines a deterministic artifact contract where every failure produces structured data with linked, inspectable evidence."
    bullets={[
      "Each failure outputs a standardized JSON artifact",
      "Evidence is captured and linked in a consistent, predictable format",
      "Artifacts are designed for automated triage and decision systems",
    ]}
    proves="Failures can be transformed into structured, machine-readable inputs, enabling automated analysis and reliable release decisions."
    accessText="The full runbook includes artifact schema, failure taxonomy, evidence contract, and rules for making failures diagnosable without rerun."
  />
</RunbookLayout>
---
import RunbookLayout from "../../../layouts/RunbookLayout.astro";
import RunbookTeaser from "../../../layouts/RunbookTeaser.astro";
---

<RunbookLayout
  title="Runbook 09 — Cypress API harness"
  description="Preview of Runbook 09 — direct API validation, deterministic environment control, and fast backend feedback."
  runbookNum="09"
>
  <RunbookTeaser
    num="09"
    title="Cypress API harness"
    intro="UI-based validation is inherently indirect. This runbook establishes a dedicated API harness that validates system behavior at the source—ensuring correctness, speed, and independence from UI constraints."
    bullets={[
      "API layer tested directly with deterministic environment configuration",
      "Positive and negative scenarios validate contract integrity and failure behavior",
      "UI dependency is removed for core system validation",
    ]}
    proves="Core system behavior can be validated independently of the UI—enabling faster feedback, clearer failures, and more reliable coverage."
    accessText="The full runbook includes harness structure, environment contract, initial API scenarios, and execution guidance for integrating backend validation into the wider system."
  />
</RunbookLayout>
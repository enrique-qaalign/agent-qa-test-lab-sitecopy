---
import RunbookLayout from "../../../layouts/RunbookLayout.astro";
import RunbookTeaser from "../../../layouts/RunbookTeaser.astro";
---

<RunbookLayout
  title="Runbook 09.5 — URL → Test Plan Agent"
  description="Preview of Runbook 09.5 — deterministic discovery from a URL into structured planning artifacts."
  runbookNum="09.5"
>
  <RunbookTeaser
    num="09.5"
    title="URL → Test Plan Agent"
    intro="Test planning is often manual, inconsistent, and difficult to scale. This runbook introduces a deterministic discovery layer that converts a system entry point into a structured understanding of capabilities, test candidates, and locators."
    bullets={[
      "URL input produces structured test and locator artifacts",
      "Capabilities are derived from real system structure, not assumptions",
      "Deterministic guardrails ensure repeatable, trustworthy outputs",
    ]}
    proves="System understanding can be automated in a controlled, repeatable way, enabling downstream automation without sacrificing reliability."
    accessText="The full runbook includes discovery mechanics, output contracts, and guardrails for turning a URL into structured planning artifacts."
  />
</RunbookLayout>
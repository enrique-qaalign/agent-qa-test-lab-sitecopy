---
import RunbookLayout from "../../../layouts/RunbookLayout.astro";
import RunbookTeaser from "../../../layouts/RunbookTeaser.astro";
---

<RunbookLayout
  title="Runbook 05 — Test data + state reset"
  description="Preview of Runbook 05 — deterministic state, idempotent runs, and parallel-safe execution."
  runbookNum="05"
>
  <RunbookTeaser
    num="05"
    title="Test data + state reset"
    intro="Most automation becomes unreliable when state is uncontrolled. This runbook establishes deterministic execution by moving setup into the API, enforcing unique data, and removing all cross-test dependencies."
    bullets={[
      "API-seeded state instead of UI setup flows",
      "Unique identifiers to eliminate collisions across runs",
      "Tests that run independently and scale to parallel execution",
    ]}
    proves="Test outcomes reflect system behavior—not environmental drift, stale data, or hidden coupling between tests."
    accessText="The full runbook includes implementation detail, command patterns, and enforcement rules used to make state deterministic and repeatable."
  />
</RunbookLayout>
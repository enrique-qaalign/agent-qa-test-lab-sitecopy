---
import RunbookLayout from "../../../layouts/RunbookLayout.astro";
import RunbookTeaser from "../../../layouts/RunbookTeaser.astro";
---

<RunbookLayout
  title="Runbook 07 — Custom fixtures + composition"
  description="Preview of Runbook 07 — fixture-owned execution, deterministic auth, and composable domain primitives."
  runbookNum="07"
>
  <RunbookTeaser
    num="07"
    title="Custom fixtures + composition"
    intro="Test systems fail when setup is distributed across tests. This runbook centralizes execution into a single fixture layer that defines authentication, environment targeting, and domain access under a controlled, deterministic contract."
    bullets={[
      "Fixture layer owns auth, API clients, and domain primitives",
      "Tests receive dependencies as inputs, with no ad hoc setup or construction",
      "Execution modes are defined at the project level instead of hidden in test logic",
    ]}
    proves="Execution becomes a controlled system boundary—enabling reliable scaling, reuse, and downstream automation."
    accessText="The full runbook includes fixture contracts, auth ownership patterns, project routing, and composition rules used to keep execution deterministic."
  />
</RunbookLayout>
---
import RunbookLayout from "../../../layouts/RunbookLayout.astro";
import RunbookTeaser from "../../../layouts/RunbookTeaser.astro";
---

<RunbookLayout
  title="Runbook 15 — CI failure triage agent"
  description="Preview of Runbook 15 — deterministic first-pass triage, evidence-first reasoning, and bounded next-step guidance."
  runbookNum="15"
>
  <RunbookTeaser
    num="15"
    title="CI failure triage agent"
    intro="Failure artifacts are only valuable if they can be interpreted quickly and consistently. This runbook introduces a deterministic triage layer that classifies failed tests, prioritizes evidence, and provides bounded next-step guidance without pretending to resolve the issue automatically."
    bullets={[
      "Structured artifacts are translated into first-pass diagnosis",
      "Reviewers are directed to the most relevant evidence first",
      "Deterministic rules control classification and prevent interpretive drift",
    ]}
    proves="Failure handling becomes operationalized—reducing confusion, shortening time-to-orientation, and creating a scalable path from signal to action."
    accessText="The full runbook includes triage logic, output shape, evidence-first review flow, and guardrails for deterministic first-pass diagnosis."
  />
</RunbookLayout>